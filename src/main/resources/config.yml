# AutoReplant Plugin Configuration

# Default state for all players (true = enabled, false = disabled)
# Players can override this per-player with /autoreplant on/off
default-enabled: true

# Seed consumption check
#   true  — One seed is required for replanting. If the crop drops contain the seed,
#           one is deducted from the drops; otherwise one is taken from the player's inventory.
#           If neither has a seed, replanting is skipped (drops still spawn normally).
#   false — Replant for free. No seed is consumed from drops or inventory.
check-seeds: true

# Bone meal: when a crop is fertilized to full maturity, auto harvest + replant (same as break-harvest flow).
#   true  — Fertilizing a crop to max age triggers harvest (drops) and replant (if enabled and seed available).
#   false — Only break-harvest triggers auto replant; bone meal has no effect.
bone-meal-auto-replant: true

# ─── Messages ────────────────────────────────────────────────────────────────
# Supports two syntaxes (can be mixed in the same string):
#
#   MiniMessage tags  — <green>, <bold>, <#ff0000>, <gradient:#f00:#00f>, ...
#   Legacy & codes    — &a, &l, &#00ff00, ...  (uppercase OK: &A, &L)
#
# Special placeholder: <command> is replaced by the actual command label
# the player used (e.g. "autoreplant" or "arp").
#
# MiniMessage reference: https://docs.advntr.dev/minimessage/format.html
# ─────────────────────────────────────────────────────────────────────────────
messages:
  prefix:        "<dark_gray>[<green>AutoReplant<dark_gray>] "
  enabled:       "<green>自動回種植 <bold>開啟"
  disabled:      "<red>自動回種植 <bold>關閉"
  usage:         "<yellow>用法: /<command> [on|off|reload]"
  no-permission: "<red>你沒有權限使用此指令。"
  console-denied: "<red>此指令只能由玩家使用。"
  reload-success: "<green>設定檔已重新載入。"
